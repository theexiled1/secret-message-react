import React from 'react';
import CreateMessageForm from './CreateMessageForm';

const Messenger = () => {
  return (
    <div>
      <CreateMessageForm />
    </div>
  );
};

export default Messenger;
