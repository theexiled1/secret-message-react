import React, { Fragment } from 'react';
import Messenger from '../Messenger/Messenger';

const Home = () => {
  return (
    <Fragment>
      <Messenger />
    </Fragment>
  );
};

export default Home;
